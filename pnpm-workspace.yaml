packages:
  - apps/landing

allowBuilds:
  esbuild: true
  sharp: true

# apps/dashboard declares Bun and is intentionally not a pnpm workspace package yet.
# Root scripts delegate to Bun for dashboard commands without changing its lockfile.

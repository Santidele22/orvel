# Archive Report: release-1-0-3-pwa (Fase 1+2)

## Why archived

Fase 1+2 of `release-1-0-3-pwa` (Tailwind build pipeline, PWA artifacts, iOS meta tags, mobile bottom nav, dashboard shell integration, stub pages, and the contract tests shipped with PR #180, commit `c1127a0`) is historical: it shipped on `main` on 2026-07-27 and is documented retroactively in the original `proposal.md` §2.1. This archive holds the pre-split folder as the record of that shipped work.

## Precedent

This archive follows the convention established by commit `d554317` (`release-2-0-cleanup-pr-2-email-outbox` → `email-outbox-cleanup`): `openspec/changes/archive/YYYY-MM-DD-<name>/<full-folder>` + `archive-report.md` + promote surviving REQs to `openspec/specs/<capability>/spec.md`.

## Where it lives now

The Fase 3+4 portion (offline walk-in queue + mobile verification) remains live at `openspec/changes/release-1-0-3-pwa/` with an edited `proposal.md` scoped to Fase 3+4 forward work only. This archive holds the original folder including the shipped Fase 1+2 content.

## What survives

No REQs were promoted to `openspec/specs/`; the Fase 3+4 REQs (`pwa-offline-walkin-queue`, `pwa-mobile-verification`) remain open under the live change folder.

## Date

2026-08-12

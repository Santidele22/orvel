// Re-export shim for the @orvel/booking migration window.
// The gateway interface lives in @orvel/booking. Kept for migration; delete after consumers migrate.
export type { SupabaseBookingGateway } from '@orvel/booking';

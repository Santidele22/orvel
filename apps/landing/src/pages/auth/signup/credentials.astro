---
import { buildInAppAuthRedirect } from '../../../lib/in-app-auth-redirect';

return Astro.redirect(
  buildInAppAuthRedirect(Astro.url, 'signup', import.meta.env.PUBLIC_DASHBOARD_URL),
  302
);
---

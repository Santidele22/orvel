---
const target = new URL('/auth/signup/account', Astro.url);
for (const [key, value] of Astro.url.searchParams.entries()) {
  target.searchParams.append(key, value);
}

return Astro.redirect(target.pathname + target.search, 301);
---

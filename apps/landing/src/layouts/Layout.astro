---
import "../styles/global.css";
import "remixicon/fonts/remixicon.css";
import "@fontsource-variable/onest";
import Preloader from "../components/organisms/Preloader.astro";

interface Props {
    title: string;
    showPreloader?: boolean;
}

const { title, showPreloader = true } = Astro.props;
const description = "Gestioná turnos y clientes con Orvel, el software simple para barberías, salones de uñas y masajes.";
---

<!DOCTYPE html>
<html lang="es" class="dark" style="scroll-behavior: smooth">
    <head>
        <meta charset="UTF-8" />
        <meta
            name="description"
            content={description}
        />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="icon" href="/favicon.ico" sizes="any" />
        <link rel="canonical" href="https://orvel.pro/" />
        <meta name="generator" content={Astro.generator} />

        <!-- SEO y Open Graph / Redes Sociales -->
        <meta name="robots" content="index, follow" />
        <meta property="og:type" content="website" />
        <meta property="og:url" content="https://orvel.pro/" />
        <meta property="og:title" content={title} />
        <meta property="og:description" content={description} />
        <meta property="og:image" content="https://orvel.pro/logo.png" />
        <meta property="og:site_name" content="Orvel" />

        <!-- Twitter / X -->
        <meta name="twitter:card" content="summary_large_image" />
        <meta name="twitter:title" content={title} />
        <meta name="twitter:description" content={description} />
        <meta name="twitter:image" content="https://orvel.pro/logo.png" />

        <title>{title}</title>
    </head>
    <body class="bg-bg-primary text-text-primary antialiased">
        {showPreloader && <Preloader />}
        <slot />

        <script>
            import { initScrollReveal } from '../lib/scroll-reveal';
            initScrollReveal();
        </script>
    </body>
</html>
